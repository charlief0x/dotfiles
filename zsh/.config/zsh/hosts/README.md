# Host Overrides

Create a folder named after `hostname -s`, then add optional files:

- `env.zsh`
- `interactive.zsh`

Example:

```text
~/.config/zsh/hosts/work-mbp/env.zsh
~/.config/zsh/hosts/work-mbp/interactive.zsh
```
